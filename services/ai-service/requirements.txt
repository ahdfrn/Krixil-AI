fastapi==0.141.1
starlette==1.3.1
uvicorn[standard]==0.32.1
pydantic==2.10.2
pydantic-settings==2.6.1
email-validator==2.2.0
sqlalchemy==2.0.36
asyncpg==0.30.0
alembic==1.14.0
passlib==1.7.4
# Pinned below 4.1: passlib 1.7.4 (last release, unmaintained) probes bcrypt.__about__,
# which bcrypt>=4.1 removed — pinning avoids that warning/detection failure entirely.
bcrypt==4.0.1
pyotp==2.9.0
python-jose[cryptography]==3.5.0
redis==5.2.0
structlog==24.4.0
httpx==0.27.2
python-multipart==0.0.32

# RAG (Phase 2)
pgvector==0.3.6
pypdf==6.16.2
python-docx==1.1.2
minio==7.2.12

# Observability (Phase 5)
prometheus-fastapi-instrumentator==8.1.0
opentelemetry-api==1.44.0
opentelemetry-sdk==1.44.0
opentelemetry-instrumentation-fastapi==0.65b0
opentelemetry-instrumentation-httpx==0.65b0
opentelemetry-exporter-otlp-proto-http==1.44.0

# MCP Hub (KIRXIL AI Stack v2 Phase 6) — the official SDK, not a hand-rolled JSON-RPC client.
# Deliberately pinned to 1.9.4, not the latest (2.x) — the latest release forces a pydantic
# upgrade (2.10.2 -> 2.13.5) as a side effect of adding this one feature, which is exactly the
# kind of broad, unrelated dependency bump this project avoids; 1.9.4 is the newest release that
# still resolves against the existing pydantic==2.10.2 pin above with zero other upgrades.
mcp==1.9.4
